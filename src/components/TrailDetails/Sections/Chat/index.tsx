import type { FC } from 'react';
import { Container } from '@/containers/TrailDetails/styles';

const Chat: FC = () => (
  <Container darkMode>
    <h1>Chat</h1>
  </Container>
);

export default Chat;
