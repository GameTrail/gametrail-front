import React from 'react';
import {
  DataText, UsersContainer,
} from './styles';

const gamegang = () => (
  <UsersContainer>
    <DataText>
      Aqui va las fotos de la gamegang
    </DataText>
  </UsersContainer>
);

export default gamegang;
