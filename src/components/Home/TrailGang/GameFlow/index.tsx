import React from 'react';
import {
  DataText, GameFlow,
} from './styles';

const gameflow = () => (
  <GameFlow>
    <DataText>
      Aqui va el carrousel de juegos
    </DataText>
  </GameFlow>
);

export default gameflow;
