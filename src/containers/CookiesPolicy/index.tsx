import React from 'react';
import { CookiesPolicy as CookiesPolicyContainer } from '@/components/CookiesPolicy';

const CookiesPolicy = () => (
  <CookiesPolicyContainer />
);

export default CookiesPolicy;
